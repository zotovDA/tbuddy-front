<!DOCTYPE html>
<html lang="en">
{{> templates/head title=title}}

<body>
  <style>
    .welcome {
      height: 100vh;
      position: relative;
      overflow: hidden;
      background: url(http://unsplash.it/id/1001/1920/1080) center no-repeat;
      background-size: cover;
    }

    .welcome-content {
      display: flex;
      height: 100%;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    .welcome-content-group {
      text-align: center;
      color: #fff;
      border: 5px solid #fff;
      padding: .5em 3em;
      background-color: rgba(0, 0, 0, .2);
      z-index: 2;
    }

    .welcome-overlay {
      position: absolute;
      top: 0;
      right: 0;
      left: 0;
      bottom: 0;
      background: url(http://unsplash.it/id/1001/1920/1080?blur) center no-repeat;
      background-size: cover;
      z-index: 0;
      opacity: 0;
    }
  </style>
  <main>
    {{> templates/header/header}}

    <div class="welcome">
      <div class="welcome-content">
        <div class="welcome-content-group">
          <h1>TRAVEL BUDDY</h1>
          <h5>A service of personal travel assistants from the city of arrival,
            <br /> who can solve any of your problems for a fee.</h5>
        </div>
      </div>
      <div class="welcome-overlay"></div>
    </div>
    <section>
      <div class="container">
        <div class="col-12 col-md-10 col-lg-8 mx-auto">
          <p class="lead">While traveling, we are faced with a bunch of problems that take up not only time but also
            resources.</p>
          <p>
            Anyone can become an assistant and start making money on the knowledge of the location of his stay. Assign
            all tasks to an assistant who lives at the place of stay.
          </p>
          <h3>How it works?</h3>
          <p>
            Enter the dates and place of the trip, choose the assistant you like, pay him a price, you talked about get
            an excellent rest without problems and hassle.</p>
          <h3>What if I am traveller?</h3>
          <p>You need to go through a simple registration and choose your assistant. Assistants are always in the city
            of your stay. All assistants speak your language and the language of the country where you arrived.</p>
          <h3>Can I become buddy?</h3>
          <p>Of course yes! If you think that your city is popular for tourists and you have something to show and tell,
            then feel free to register in our service.</p>
        </div>
      </div>
    </section>
  </main>

  {{> templates/footer}}
  <script>
    /**
    * Parallax scroll effect
    * Created by Sallar Kaboli <sallar.kaboli@gmail.com>
    */
    var $content = document.querySelector('.welcome .welcome-content')
      , $blur = document.querySelector('.welcome .welcome-overlay')
      , wHeight = window.innerHeight;

    window.addEventListener('resize', function () {
      wHeight = window.innerHeight;
    });

    /**
     * requestAnimationFrame Shim 
     */
    window.requestAnimFrame = (function () {
      return window.requestAnimationFrame ||
        window.webkitRequestAnimationFrame ||
        window.mozRequestAnimationFrame ||
        function (callback) {
          window.setTimeout(callback, 1000 / 60);
        };
    })();

    /**
     * Scroller
     */
    function Scroller() {
      this.latestKnownScrollY = 0;
      this.ticking = false;
    }

    Scroller.prototype = {
      /**
       * Initialize
       */
      init: function () {
        window.addEventListener('scroll', this.onScroll.bind(this), false);
      },

      /**
       * Capture Scroll
       */
      onScroll: function () {
        this.latestKnownScrollY = window.scrollY;
        this.requestTick();
      },

      /**
       * Request a Tick
       */
      requestTick: function () {
        if (!this.ticking) {
          window.requestAnimFrame(this.update.bind(this));
        }
        this.ticking = true;
      },

      /**
       * Update.
       */
      update: function () {
        var currentScrollY = this.latestKnownScrollY;
        this.ticking = false;

        /**
         * Do The Dirty Work Here
         */
        var slowScroll = currentScrollY / 4,
          blurScroll = currentScrollY * 2;

        $content.style.transform = 'translateY(-' + slowScroll + 'px)';

        $blur.style.opacity = blurScroll / wHeight;
      }
    };

    /**
     * Attach!
     */
    var scroller = new Scroller();
    scroller.init();
  </script>
</body>

</html>