Your buddy is: {{ name }}
<div class="text-center"> <a data-toggle="collapse" href="#buddyDetails-{{ id }}" role="button" aria-expanded="false"
    aria-controls="buddyDetails" class="small">
    details
  </a></div>

<div class="collapse" id="buddyDetails-{{ id }}">
  <div class="card card-body">
    {{ contacts }}
    {{#unless isDone}}
    <button data-request="{{ id }}" class="js-solve-button btn btn-primary mt-3">Solved</button>
    {{/unless}}
  </div>
</div>